<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="egovframework.example.cmmn.MyList.service.ListMapper">
	
	<!-- 게시글 목록 조회 -->
	<select id="selectList"
		parameterType="egovframework.example.cmmn.MyList.vo.Search"
		resultType="egovframework.example.cmmn.MyList.vo.ListVo">
		SELECT * FROM test
		<where>
			<if test="searchType=='testTitle' and keyword != null and keyword !=''">
				AND testTitle like CONCAT('%',#{keyword}, '%')
			</if>
			<if test="searchType=='testContent' and keyword != null and keyword !=''">
				AND testContent like CONCAT('%',#{keyword}, '%')
			</if><if test="searchType=='testName' and keyword != null and keyword !=''">
				AND testName like CONCAT('%',#{keyword}, '%')
			</if>
		</where>
		ORDER BY testId DESC
		LIMIT #{startList}, #{listSize}
	</select>
	
	<!-- 게시글 갯수 -->
	<select id="getBoardListCnt" parameterType="egovframework.example.cmmn.MyList.vo.Search" resultType="Integer">
		SELECT count(*) as listCnt
		FROM test
		<where>
			<if test="keyword != null and keyword !=''">
				<if test="searchType=='testTitle'">
					AND testTitle like CONCAT('%',#{keyword}, '%')
				</if>
				<if test="searchType=='testContent'">
					AND testContent like CONCAT('%',#{keyword}, '%')
				</if>
				<if test="searchType=='testName'">
					AND testName like CONCAT('%',#{keyword}, '%')
				</if>
			</if>
		</where>
	</select>
	
	<!-- 게시글 상세보기 -->
	<select id="selectDetail" parameterType="Integer" resultType="egovframework.example.cmmn.MyList.vo.ListVo">
		SELECT * FROM test
		WHERE testId = #{testId}
	</select>
	
	<!-- 게시글 쓰기 -->
	<insert id="insertList" parameterType="egovframework.example.cmmn.MyList.vo.ListVo">
		INSERT INTO test(testTitle, testContent, testName, testDate, password)
		VALUES(#{testTitle},#{testContent},#{testName},now(), #{password, jdbcType=VARCHAR})
	</insert>
	
	<!-- 게시글 수정 -->
	<update id="updateList" parameterType="egovframework.example.cmmn.MyList.vo.ListVo">
		UPDATE test SET testTitle = #{testTitle}, testContent = #{testContent}
		WHERE testId = #{testId} AND password = #{password, jdbcType=VARCHAR}
	</update>
	
	<!-- 게시글 삭제 -->
	<delete id="deleteList" parameterType="egovframework.example.cmmn.MyList.vo.ListVo">
		DELETE FROM test
		WHERE testId = #{testId} AND password = #{password, jdbcType=VARCHAR}
	</delete>
	
</mapper>
